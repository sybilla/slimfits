{
  "version": "0.2.0",
  "configurations": [{
    "name": "Debug",
    "type": "node",
    "request": "launch",
    "program": "${workspaceRoot}/node_modules/jest/bin/jest.js",
    "protocol": "inspector",
    "stopOnEntry": false,
    "args": [
      "--env=jsdom",
      "--no-cache",
      "--runInBand",
      "--findRelatedTests",
      "${file}"
    ],
    "skipFiles": [
      "<node_internals>/**/*.js"
    ]
  }]
}
